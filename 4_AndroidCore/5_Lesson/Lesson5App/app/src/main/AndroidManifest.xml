<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.fortaestro.lesson5app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lesson5App">
        <activity android:name=".timer.TimerActivity"></activity>
        <activity android:name=".observer.ObserverActivity" />
        <activity android:name=".fragmentmanager.FragmentManagerActivity" />
        <activity android:name=".cityheraldry.CityHeraldryActivity" />
        <activity android:name=".cityheraldry.CoatOfArmsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>